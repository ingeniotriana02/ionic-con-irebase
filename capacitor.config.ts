import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Ionic con FireBase',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
